# CollabHub

Collaboration network for students and professionals — profiles, connections, messaging, and ideas.

## Run locally

```bash
npm install
npm run dev
```

Configure Firebase (and Supabase for OAuth callback) via environment variables as required by `src/config/`.

## Scripts

- `npm run dev` — development server
- `npm run build` — production build
- `npm run preview` — preview production build
